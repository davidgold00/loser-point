"""Shared visual theme and pure figure-generating functions.

Every figure function takes data in and saves both PNG (README) and SVG
(memo) to disk using the shared theme, with a source+n footer annotation.
"""
